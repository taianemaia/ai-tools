# <JIRA-KEY> Implementation Plan — <story short title>

> Generated by plan-auditor. Iteration [N]. Design: `[path/to/tech-design.md]`

---

## What this story does

[2–3 sentences. What is being built, what it enables, and the one architectural
constraint that most shapes the implementation.]

## Target state

```
[repo-root]/
  [path/to/file]      NEW
  [path/to/file]      MODIFY
  [path/to/file]      DELETE
```

---

## Steps

Follow in order.

---

### Step 1 — [Short verb phrase]

**File:** `[exact/path/to/file.ts]` *(new)*

[One sentence on what this step creates and why it must come first.]

```typescript
// complete, compilable code for this file or change
```

---

### Step 2 — [Short verb phrase]

**File:** `[exact/path/to/file.ts]` *(modify — add to existing [section/object/class])*

[One sentence on what changes and why.]

```typescript
// the change — show only the affected lines plus enough context to locate them
```

---

*(Repeat for each step. Steps that can be done in any order are grouped under
a single heading: "Steps N–M (order-independent)")*

---

## Test plan

Write tests in `[exact/path/to/test-file.ts]`.

| Test case | What to assert |
|---|---|
| [Name] | [Exact assertion — e.g. "`httpClient.request` called with `headers: { 'X-Channel': 'web' }`"] |
| [Name] | [Assertion] |

Mock setup: [one sentence on how to mock the dependencies, referencing the
closest existing test file as the pattern, e.g. "Same mock setup as
`packages/store-core/components/header/header.test.tsx`"].

---

## How to verify

```bash
# run after completing all steps
[exact command 1]
[exact command 2]
[exact command 3]
```

All three must exit 0 before opening a PR.

---

## AC traceability

| AC | File | Test case |
|---|---|---|
| [AC ID + one-liner] | `[file path]` | `[test case name]` |

---

## Skipped acceptance criteria

*(Only present when one or more ACs were confirmed out of scope for this story.)*

These ACs appear in the story but are **not implemented here**. Listed so
developers and reviewers know they were seen and deliberately deferred.

| AC | Reason | Owner story |
|---|---|---|
| [AC text] | Out of scope — confirmed by [person] on [date] | [JIRA-KEY] |

---

## Advisory notes

*(Only present if the plan-auditor found NOTE-severity items.)*

- **[Label]**: [one sentence describing the advisory and the recommended action]
